import GradebookPage from '@/components/teacher/gradebook/GradebookPage';

export const metadata = {
  title: 'Gradebook — Teacher Portal | Sidelile High School',
};

export default function Page() {
  return <GradebookPage />;
}
