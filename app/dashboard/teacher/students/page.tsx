import StudentsListPage from '@/components/teacher/students/StudentsListPage';

export const metadata = {
  title: 'Students — Teacher Portal | Sidelile High School',
};

export default function Page() {
  return <StudentsListPage />;
}
